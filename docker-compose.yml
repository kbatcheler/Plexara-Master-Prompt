# Local production-like stack for Plexara.
#
#   docker compose up --build
#
# Spins up the app container + a Postgres 16 instance, with a persistent
# volume for uploads (so STORAGE_PROVIDER=local survives restarts).
# This is the surface the migration developer will replicate on Cloud Run /
# ECS / Kubernetes once cloud Postgres + cloud object storage are wired in.

services:
  # One-shot migration runner. Applies committed Drizzle migrations from
  # /app/drizzle and exits 0. The `app` service waits for this to succeed
  # before booting so the schema is always at least as new as the code.
  # In Cloud Run / ECS this is the equivalent of a pre-deploy job or init
  # container; in Kubernetes it would be a Job referenced by the Deployment.
  migrate:
    build: .
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://plexara:plexara@db:5432/plexara
    depends_on:
      db:
        condition: service_healthy
    # The runtime image ships the SQL files at /app/drizzle and the
    # migrate runner from @workspace/db. We invoke node directly against
    # the bundled migrate script so we don't need pnpm in the runtime image.
    entrypoint: ["node", "--enable-source-maps", "/app/dist/migrate.mjs"]
    restart: "no"

  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      PORT: "8080"
      LOG_LEVEL: info
      DATABASE_URL: postgresql://plexara:plexara@db:5432/plexara
      STORAGE_PROVIDER: local
      FILE_STORAGE_PATH: /app/uploads
      # The remainder must come from your local .env file (not committed).
      # Compose merges .env from the project root automatically.
      SESSION_SECRET: ${SESSION_SECRET}
      PHI_ENCRYPTION_KEY: ${PHI_ENCRYPTION_KEY}
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY}
      CLERK_PUBLISHABLE_KEY: ${CLERK_PUBLISHABLE_KEY}
      AI_INTEGRATIONS_ANTHROPIC_API_KEY: ${AI_INTEGRATIONS_ANTHROPIC_API_KEY}
      AI_INTEGRATIONS_ANTHROPIC_BASE_URL: ${AI_INTEGRATIONS_ANTHROPIC_BASE_URL}
      AI_INTEGRATIONS_OPENAI_API_KEY: ${AI_INTEGRATIONS_OPENAI_API_KEY}
      AI_INTEGRATIONS_OPENAI_BASE_URL: ${AI_INTEGRATIONS_OPENAI_BASE_URL}
      AI_INTEGRATIONS_GEMINI_API_KEY: ${AI_INTEGRATIONS_GEMINI_API_KEY}
      AI_INTEGRATIONS_GEMINI_BASE_URL: ${AI_INTEGRATIONS_GEMINI_BASE_URL}
      CORS_ORIGIN: http://localhost:8080
    volumes:
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/api/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: plexara
      POSTGRES_PASSWORD: plexara
      POSTGRES_DB: plexara
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U plexara -d plexara"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  uploads:
